name: Test labels

on: 
  [push, pull_request]


jobs:
  nightly_build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]
    if: ${{ contains(github.event.pull_request.labels.*.name, 'nightly-build') && startsWith(matrix.os, 'ubuntu') }}
    steps:
    - name: List lables in PR
      run: | 
        echo "Nightly build label"
